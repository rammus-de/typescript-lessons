import {sample as randomSample}  from './utils.js'
import type {Person } from  './types'
import add from './utils'
import {pi} from './utils'
const sample = 1234
const player1:Person = {email:"e@@.com",username:"mkdir"}
console.log(randomSample([23,4,5]))
console.log(add(1,2))
console.log(pi)
